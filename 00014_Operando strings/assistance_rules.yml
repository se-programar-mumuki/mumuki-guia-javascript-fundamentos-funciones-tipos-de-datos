---
rules:
- !ruby/hash:ActiveSupport::HashWithIndifferentAccess
  when: !ruby/hash:ActiveSupport::HashWithIndifferentAccess
    these_tests_failed:
    - longitudNombreCompleto no falta contemplar el espacio
  then: Parece que no estás contemplando el espacio; la `longitudNombreCompleto` debe
    devolver `1` más que las longitudes sumadas de `nombre` y `apellido`

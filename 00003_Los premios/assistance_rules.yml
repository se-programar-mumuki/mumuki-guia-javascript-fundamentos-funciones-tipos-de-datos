---
rules:
- !ruby/hash:ActiveSupport::HashWithIndifferentAccess
  when: !ruby/hash:ActiveSupport::HashWithIndifferentAccess
    these_expectations_failed:
    - "* Declares:medallaSegunPuesto"
  then: 'cuidado, tenés que definir `medallaSegunPuesto` :medal:'
- !ruby/hash:ActiveSupport::HashWithIndifferentAccess
  when: !ruby/hash:ActiveSupport::HashWithIndifferentAccess
    these_expectations_failed:
    - medallaSegunPuesto UsesIf
  then: 'si el puesto es `1` tenés que devolver `"oro"`, si el puesto es `2`, tenés
    que devolver `"plata"` y así; quizás acá un `if` pueda ayudarnos :smile:'
